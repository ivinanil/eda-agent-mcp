# EDA Report: Hotel_reservation.csv
*Generated automatically by EDA Agent on March 31, 2026 at 14:27*

---

# Exploratory Data Analysis Report
## Dataset: Hotel_reservation.csv

*Report generated for business and data science audience*

---

## 1. Dataset Overview

The Hotel Reservation dataset contains **36,275 booking records** across **19 columns**, capturing guest behavior, booking logistics, pricing, and outcomes for a hotel property spanning arrivals in **2017 and 2018**.

| Attribute | Detail |
|---|---|
| Total Records | 36,275 |
| Total Features | 19 |
| Numeric Features | 14 |
| Categorical Features | 5 |
| Missing Values | **None detected** |
| Duplicate Rows | **None detected** |
| Target Variable | `booking_status` (Binary: Canceled / Not_Canceled) |
| Date Range | Arrival years 2017–2018 |

The dataset is structured around a **binary classification problem**: predicting whether a hotel booking will be canceled. The clean state (no nulls, no duplicates) suggests this data has undergone prior preprocessing, though several quality concerns discussed below still warrant attention.

`Booking_ID` is a unique identifier (36,275 unique values across 36,275 rows) and carries no predictive signal — it should be **excluded from all modeling**.

---

## 2. Data Quality Assessment

![Distributions](charts/distributions.png)

### ✅ No Missing Values — With Caveats

While the system reports zero missing values, several columns exhibit distributions that strongly suggest **implicit missing or erroneous values masquerading as valid entries**:

| Column | Issue | Severity |
|---|---|---|
| `avg_price_per_room` | Minimum value = **0.0** — rooms priced at $0 are likely data entry errors or complimentary stays miscoded | 🔴 HIGH |
| `no_of_adults` | Minimum value = **0** — a booking with zero adults and potentially zero children is logically invalid | 🔴 HIGH |
| `no_of_children` | Maximum value = **10** with extreme kurtosis (36.98) — outliers likely reflect data errors | 🟡 MEDIUM |
| `no_of_week_nights` + `no_of_weekend_nights` | Both can be 0 simultaneously — a booking with 0 total nights is suspicious | 🟡 MEDIUM |
| `no_of_previous_cancellations` | Max = **13**, skewness = **25.20**, kurtosis = **732.74** — extreme outliers suggesting a small group of serial cancellers or data anomalies | 🟡 MEDIUM |
| `no_of_previous_bookings_not_canceled` | Max = **58**, kurtosis = **457.38** — similarly extreme; a guest with 58 prior bookings is an outlier requiring verification | 🟡 MEDIUM |
| `Meal Plan 3` | Only **5 bookings** selected this option out of 36,275 — near-zero variance, negligible category | 🟡 MEDIUM |

### Key Quality Actions Required

1. **Investigate `avg_price_per_room = 0`**: Cross-reference with `market_segment_type = "Complementary"` (391 records). If the overlap is smaller than the zero-price count, excess records are likely errors.
2. **Validate zero-adult bookings**: These 155 outliers in `no_of_adults` may represent corporate block bookings or errors.
3. **Cap or bin extreme guest history columns**: The `no_of_previous_cancellations` and `no_of_previous_bookings_not_canceled` distributions are so right-skewed that raw values will distort tree-based and linear models alike.

---

## 3. Key Statistical Insights

### Booking Occupancy Patterns

- The **average booking is for ~1.85 adults** (median = 2), indicating predominantly couples or pairs — solo travelers are relatively rare.
- **Children are uncommon**: mean = 0.105, median = 0 — over 90% of bookings involve no children. The max of 10 children in one booking is almost certainly erroneous and should be investigated.
- The average stay involves **~0.81 weekend nights + ~2.20 week nights**, totaling roughly 3 nights. This is a primarily weekday/business-midweek property or the booking mix skews toward short mid-week stays.

### Pricing Distribution

- `avg_price_per_room` has a mean of **$103.42** and median of **$99.45**, suggesting a modest right skew ($4 gap). However, the maximum of **$540** and presence of **$0 values** create a wide effective range.
- The standard deviation of **$35.09** reflects meaningful pricing variation — room type and guest count are likely key price drivers (confirmed in correlations below).
- 357 statistical outliers in pricing deserve review before modeling.

### Lead Time Behavior

- `lead_time` (days between booking and arrival) has a mean of **85 days** but a median of only **57 days**, confirming heavy right skew — most guests book within 2 months, but a meaningful tail books very far in advance (up to 443 days, ~14.5 months out).
- This variable is likely a **strong cancellation predictor**: guests who book far in advance have more opportunities and motivation to cancel.

### Guest Loyalty Indicators

- Only **2.56% of guests are repeat visitors** (`repeated_guest` mean = 0.026), meaning this hotel has a predominantly first-time or transient guest base — typical for resort/leisure properties.
- A mere **2.33% of bookings** have any prior cancellation on record, yet those that do reach up to 13 prior cancellations — a highly concentrated bad-actor segment.

### Temporal Coverage

- The data spans exactly **2017–2018** with mean arrival year of 2017.82, indicating roughly **18% of arrivals in 2017** and **82% in 2018**. This imbalance means year-based time series validation must be handled carefully to avoid data leakage.
- `arrival_month` distribution (mean = 7.42, median = 8) confirms a **summer-heavy booking pattern**, with lower activity in early months — a negative kurtosis of -0.93 suggests a relatively flat, spread-out distribution across months without sharp peaks.

---

## 4. Correlation Analysis

![Correlation Heatmap](charts/correlation_heatmap.png)

### Top Correlations Summary

| Feature A | Feature B | Correlation | Interpretation |
|---|---|---|---|
| `repeated_guest` | `no_of_previous_bookings_not_canceled` | **0.539** | Expected logical relationship — repeat guests have more completed stays |
| `no_of_previous_cancellations` | `no_of_previous_bookings_not_canceled` | **0.468** | Active repeat customers also accumulate more cancellations — high-volume guests |
| `repeated_guest` | `no_of_previous_cancellations` | **0.391** | Repeat guests have more cancellation history — volume effect |
| `arrival_year` | `arrival_month` | **-0.340** | 2017 data skews toward later months (hotel opened mid-year or data collection began mid-2017) |
| `no_of_children` | `avg_price_per_room` | **0.338** | Family bookings command higher room rates — possibly larger room types |
| `no_of_adults` | `avg_price_per_room` | **0.297** | More adults = higher pricing, consistent with per-person charges |
| `no_of_adults` | `no_of_special_requests` | **0.189** | Larger groups make more requests — operationally important |

### ⚠️ Multicollinearity Assessment

**No correlation exceeds the 0.70 threshold** for severe multicollinearity. However, the **cluster of guest-history variables** (`repeated_guest`, `no_of_previous_cancellations`, `no_of_previous_bookings_not_canceled`) are moderately intercorrelated (0.39–0.54). For linear or logistic regression models, consider:
- Using only `repeated_guest` as a binary flag and binning the count variables, OR
- Creating a single composite "guest loyalty/risk score" feature

The `arrival_year`/`arrival_month` correlation (-0.34) is an artifact of data collection timing and is not a true seasonal signal — be cautious when engineering date features.

---

## 5. Categorical Variable Analysis

![Meal Plan Distribution](charts/categorical_type_of_meal_plan.png)
![Room Type Distribution](charts/categorical_room_type_reserved.png)

### `booking_status` — Target Variable

| Status | Count | Percentage |
|---|---|---|
| Not_Canceled | 24,390 | **67.2%** |
| Canceled | 11,885 | **32.8%** |

A **32.8% cancellation rate** is substantial — this is a meaningful business problem. The class imbalance, while not severe, is sufficient to make accuracy a **misleading metric**. Models must be evaluated using **AUC-ROC, F1-score, or Precision-Recall curves**. A naive classifier predicting "Not Canceled" every time would achieve 67.2% accuracy, setting the performance baseline.

### `type_of_meal_plan`

| Meal Plan | Count | % of Bookings |
|---|---|---|
| Meal Plan 1 | 27,835 | 76.7% |
| Not Selected | 5,130 | 14.1% |
| Meal Plan 2 | 3,305 | 9.1% |
| Meal Plan 3 | **5** | **0.01%** |

**Meal Plan 3 has only 5 records** — this near-zero variance category will cause instability in models and should be **merged with "Meal Plan 2" or dropped** before encoding.

### `room_type_reserved`

| Room Type | Count | % of Bookings |
|---|---|---|
| Room_Type 1 | 28,130 | 77.6% |
| Room_Type 4 | 6,057 | 16.7% |
| Room_Type 6 | 966 | 2.7% |
| Room_Type 2 | 692 | 1.9% |
| Room_Type 5 | 265 | 0.7% |
| Room_Type 3/7 | ~165 | <0.5% |

**Room_Type 1 dominates at 77.6%**. Rare room types (Types 3, 5, 7) will have sparse representation in train/test splits — consider **consolidating low-frequency types** into an "Other" category for modeling stability.

### `market_segment_type`

| Segment | Count | % |
|---|---|---|
| Online | 23,214 | 64.0% |
| Offline | 10,528 | 29.0% |
| Corporate | 2,017 | 5.6% |
| Complementary | 391 | 1.1% |
| Aviation | 125 | 0.3% |

**Online bookings represent 64% of volume** — the digital channel dominates. Aviation (125 records, 0.3%) is extremely sparse and warrants consolidation with "Offline" or "Other" for modeling. The `Complementary` segment (391 records, 1.1%) should be cross-validated against the zero-price room anomaly noted in Section 2.

---

## 6. ML Readiness Assessment

### Target Variable
The binary `booking_status` column is well-suited for **binary classification**. At 32.8% positive rate (cancellations), moderate class imbalance strategies (SMOTE, class weighting, or threshold tuning) should be considered.

### Feature Readiness Summary

| Feature | ML Ready? | Action Required |
|---|---|---|
| `Booking_ID` | ❌ No | Drop — unique identifier, zero predictive value |
| `arrival_year` | ⚠️ Caution | Only 2 values; treat as binary or drop if overfitting |
| `no_of_previous_cancellations` | ⚠️ Caution | Log-transform or bin due to extreme skew (25.20) |
| `no_of_previous_bookings_not_canceled` | ⚠️ Caution | Log-transform or bin due to extreme skew (19.25) |
| `no_of_children` | ⚠️ Caution | Cap at realistic maximum (e.g., 4); investigate max=10 |
| `avg_price_per_room` | ⚠️ Caution | Investigate zero values; log-transform recommended |
| `lead_time` | ✅ Ready | Log-transform recommended to reduce right skew |
| `type_of_meal_plan` | ⚠️ Caution | Merge Meal Plan 3 into another category before encoding |
| `room_type_reserved` | ⚠️ Caution | Consolidate rare room types |
| `market_segment_type` | ⚠️ Caution | Consolidate Aviation (125 records) |
| `required_car_parking_space` | ✅ Ready | Binary flag, already 0/1 encoded |
| `repeated_guest` | ✅ Ready | Binary flag, already 0/1 encoded |
| All other numerics | ✅ Ready | Minimal preprocessing needed |

### Recommended Encoding Strategy
- **Ordinal encoding**: `type_of_meal_plan` (natural ordering exists), `no_of_special_requests`
- **One-hot encoding**: `market_segment_type`, `room_type_reserved`
- **Binary**: `required_car_parking_space`, `repeated_guest`, `booking_status` (already binary)

### Algorithm Suitability

| Algorithm | Suitability | Rationale |
|---|---|---|
| **Gradient Boosting (XGBoost/LightGBM)** | 🟢 Excellent | Handles skew, mixed types, class imbalance natively |
| **Random Forest** | 🟢 Good | Robust to outliers; no scaling needed |
| **Logistic Regression** | 🟡 Moderate | Requires scaling, log-transforms, and careful handling of skewed features |
| **Neural Networks** | 🟡 Moderate | Requires normalization; limited by tabular size (~36K rows) |
| **K-Nearest Neighbors** | 🔴 Poor | Sensitive to skewed, high-range features like `lead_time` |

---

## 7. Recommended Next Steps

### Immediate Data Cleaning (Before Any Modeling)

1. **Audit zero-price bookings**: Run `avg_price_per_room == 0` and cross-tabulate with `market_segment_type`. Flag non-Complementary zero-price records for removal or imputation with segment-median pricing.

2. **Remove or cap `no_of_children` outliers**: Values above 4 are implausible for standard hotel bookings. Investigate and cap at 4, or treat max=10 as a data entry error (e.g., adult count entered in child field).

3. **Validate zero-adult bookings**: Filter `no_of_adults == 0` and determine if these are legitimate (e.g., infant-only bookings, system errors). If no business justification, remove these 155 records.

4. **Merge sparse categories**:
   - `Meal Plan 3` → merge with `Meal Plan 2`
   - `Aviation` market segment → merge with `Offline` or create "Other"
   - Consolidate `room_type_reserved` categories with fewer than 300 bookings into "Other"

### Feature Engineering Priorities

5. **Create `total_nights` = `no_of_weekend_nights` + `no_of_week_nights`**: A single stay-length feature will be more interpretable and predictive than two correlated night-count columns.

6. **Create a full arrival date**: Combine `arrival_year`, `arrival_month`, and `arrival_date` into a single datetime feature, then engineer **day-of-week**, **quarter**, and **days-to-holiday** features which will capture seasonality more effectively.

7. **Log-transform right-skewed features**: Apply `log1p` transformation to `lead_time`, `no_of_previous_cancellations`, `no_of_previous_bookings_not_canceled`, and `avg_price_per_room` prior to linear model training.

8. **Create a "guest risk score"**: Combine `repeated_guest`, `no_of_previous_cancellations`, and `no_of_previous_bookings_not_canceled` into a composite feature encoding guest reliability.

### Modeling Strategy