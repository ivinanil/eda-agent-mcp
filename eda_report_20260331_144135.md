# EDA Report: Hotel_reservation.csv
*Generated automatically by EDA Agent on March 31, 2026 at 14:41*

---

# Exploratory Data Analysis Report
## Dataset: Hotel_reservation.csv

*Report generated for business and data science audiences*

---

## 1. Dataset Overview

The hotel reservations dataset contains **36,275 booking records** across **19 features**, covering reservations made for arrivals in **2017 and 2018**. The dataset is well-structured, with a clean mix of numeric and categorical variables that collectively describe guest profiles, booking behavior, room preferences, and ultimately whether a reservation was honored or cancelled.

| Attribute | Value |
|---|---|
| Total Records | 36,275 |
| Total Features | 19 |
| Numeric Features | 14 |
| Categorical Features | 5 |
| Missing Values | 0 |
| Duplicate Rows | 0 |
| Target Variable | `booking_status` |
| Coverage Period | 2017 – 2018 |

The dataset's primary business value lies in its ability to support **cancellation prediction** — a critical use case for hotel revenue management. Knowing in advance whether a booking is likely to cancel allows hotels to optimize overbooking strategies, staffing, and dynamic pricing.

---

## 2. Data Quality Assessment

### ✅ Missing Values
Remarkably, **no missing values were detected across any of the 19 columns**. This is highly unusual for real-world hospitality data and may indicate either thorough data collection practices or upstream imputation/filtering that should be investigated before assuming the dataset is pristine.

### ✅ Duplicate Records
**Zero duplicate rows** were found. Combined with the fact that `Booking_ID` has 36,275 unique values (matching the row count exactly), each record reliably represents one unique booking transaction.

### ⚠️ Data Quality Flags Requiring Attention

**1. `avg_price_per_room` — Minimum value of 0.0 (HIGH PRIORITY)**
The minimum room price is **$0.00**, which is almost certainly not a legitimate market rate. With a mean of $103.42 and a max of $540.00, zero-dollar bookings likely represent complimentary rooms, data entry errors, or internal bookings. These records should be cross-referenced against `market_segment_type = "Complementary"` (391 records) — but if zero prices exceed 391, additional investigation is needed. Zero-price rooms can distort revenue models significantly.

**2. `no_of_children` — Max value of 10 with extreme skewness (kurtosis: 36.98)**
The vast majority of bookings have 0 children (median = 0, mean = 0.105), but the maximum of **10 children** is suspicious. A family booking with 10 children is an extreme outlier and may represent a data entry error. The 1,080 flagged outliers in this column deserve manual review.

**3. `no_of_adults` — Minimum value of 0**
A hotel booking with **0 adults** does not make business sense. Even child-only scenarios would typically require an adult guardian. These records should be audited — they likely represent data quality issues or very unusual edge cases.

**4. `no_of_previous_cancellations` — Extreme kurtosis of 732.74**
This column has a kurtosis of **732.74** and skewness of **25.20** — one of the most extreme distributions in the dataset. While most guests have 0 previous cancellations, a tiny number have up to 13. These heavy-tailed outliers will heavily distort any linear model and require robust treatment.

**5. `arrival_year` — Only two values (2017, 2018)**
This column has near-zero variance (std = 0.38) and only two possible values. It contributes minimal predictive signal as a continuous variable and should be treated as a **binary indicator** or dropped depending on model goals. The negative correlation with `arrival_month` (–0.34) suggests the dataset skews toward later months in 2017 and earlier months in 2018 — a temporal artifact of the data collection window, not a true relationship.

**6. `Meal Plan 3` — Only 5 records**
Out of 36,275 reservations, only **5 selected Meal Plan 3**. This near-empty category will cause issues in train/test splits and cross-validation folds. It should be merged with another category (likely "Not Selected" or "Meal Plan 2") or handled with stratified sampling.

**Summary of Data Quality Flags:**

| Column | Issue | Priority | Recommended Action |
|---|---|---|---|
| `avg_price_per_room` | Min = $0.00 | HIGH | Investigate zero-price records; consider filtering or flagging |
| `no_of_children` | Max = 10, kurtosis = 36.98 | HIGH | Cap or Winsorize at 95th percentile |
| `no_of_adults` | Min = 0 | MEDIUM | Audit zero-adult bookings |
| `no_of_previous_cancellations` | Kurtosis = 732.74 | HIGH | Log-transform or bin into categories |
| `no_of_previous_bookings_not_canceled` | Max = 58, kurtosis = 457.38 | HIGH | Log-transform or bin |
| `arrival_year` | Only 2 values | LOW | Convert to binary or drop |
| `type_of_meal_plan` = "Meal Plan 3" | Only 5 records | MEDIUM | Merge with similar category |

---

## 3. Key Statistical Insights

![Numeric Distributions](charts/distributions.html)

### Booking Lead Time
`lead_time` ranges from **0 to 443 days**, with a mean of **85.2 days** and a median of **57 days**. The right skew (skewness = 1.29) indicates that while most bookings are made roughly 2 months in advance, a meaningful tail of guests books very far ahead (6–14 months). Lead time is typically one of the strongest predictors of cancellation risk — longer lead times are associated with higher cancellation rates — making this a **critical feature** to include in any predictive model.

### Room Pricing
`avg_price_per_room` has a mean of **$103.42** and a median of **$99.45**, suggesting a relatively symmetric distribution around the $100 mark with a moderate right skew (0.67) from luxury rooms reaching up to **$540**. The 357 outliers at the high end likely represent premium room types or peak-season pricing and are legitimate rather than erroneous.

### Guest Composition
- The average booking is for **1.85 adults** (median = 2), consistent with predominantly couple-based leisure travel.
- Only **10.5% of bookings include children** (mean = 0.105), making this a largely adult/couple-oriented property.
- **93.1% of guests have never previously stayed** (repeated_guest mean = 0.0256), suggesting either a tourist-heavy clientele or poor loyalty program penetration.

### Stay Duration
- Average **weekend nights = 0.81** vs average **weekday nights = 2.20**, confirming this hotel serves a mix of weekend leisure and weekday business travelers.
- The maximum stay of **17 weekday nights** is an outlier and may represent long-term corporate stays.

### Special Requests
`no_of_special_requests` has a mean of **0.62** with a max of 5. Given that special requests are often correlated with guest engagement and commitment to the stay, this variable is likely a strong negative predictor of cancellation.

### Car Parking
Only **3.1% of guests** requested parking (mean = 0.031), which is unexpectedly low. This either reflects an urban property with poor parking facilities or a guest profile (international tourists, transit users) that doesn't require vehicles.

---

## 4. Correlation Analysis

![Correlation Heatmap](charts/correlation_heatmap.html)

### No Multicollinearity Concerns
**No correlation pair exceeds the 0.7 multicollinearity threshold**, which is a positive finding for linear model stability. The strongest correlation observed is 0.54.

| Feature A | Feature B | Correlation | Interpretation |
|---|---|---|---|
| `repeated_guest` | `no_of_previous_bookings_not_canceled` | **0.539** | Expected: loyal guests have more prior stays — these capture overlapping loyalty signals |
| `no_of_previous_cancellations` | `no_of_previous_bookings_not_canceled` | **0.468** | High-frequency bookers tend to have both more cancellations and more completions |
| `repeated_guest` | `no_of_previous_cancellations` | **0.391** | Returning guests may have mixed history records |
| `arrival_year` | `arrival_month` | **–0.340** | Artifact of data collection window, not a meaningful business relationship |
| `no_of_children` | `avg_price_per_room` | **0.338** | Family bookings command higher room rates (larger rooms, suites) |
| `no_of_adults` | `avg_price_per_room` | **0.297** | Larger groups pay more; consistent with the children finding above |
| `avg_price_per_room` | `no_of_special_requests` | **0.184** | Higher-paying guests make more requests — premium guest behavior pattern |

### Key Observations:
1. **Loyalty cluster**: `repeated_guest`, `no_of_previous_cancellations`, and `no_of_previous_bookings_not_canceled` form a loosely correlated trio. While below the multicollinearity threshold, consider whether all three are needed in tree-based models vs. linear models.
2. **Party size → price signal**: The positive correlations between guest count (adults + children) and `avg_price_per_room` are genuine business relationships and should be preserved as features.
3. **The year-month artifact** (–0.34) should be managed by treating dates as cyclical or temporal features rather than raw integers.

---

## 5. Categorical Variable Analysis

![Meal Plan Distribution](charts/categorical_type_of_meal_plan.html)
![Room Type Distribution](charts/categorical_room_type_reserved.html)

### Booking Status (Target Variable)
| Status | Count | Percentage |
|---|---|---|
| Not Canceled | 24,390 | **67.2%** |
| Canceled | 11,885 | **32.8%** |

A **32.8% cancellation rate** is notably high (industry averages typically range 20–40% for online bookings). This represents significant revenue leakage. The class imbalance is moderate — a 2:1 ratio is manageable without aggressive resampling, but techniques like SMOTE or class-weight adjustment should still be considered for model training.

### Market Segment
| Segment | Count | Share |
|---|---|---|
| Online | 23,214 | 64.0% |
| Offline | 10,528 | 29.0% |
| Corporate | 2,017 | 5.6% |
| Complementary | 391 | 1.1% |
| Aviation | 125 | 0.3% |

Online bookings dominate at **64%**, which aligns with industry trends toward OTA (Online Travel Agency) dominance. The "Aviation" and "Complementary" segments are very small and may need to be grouped as "Other" to avoid sparse category problems in models.

### Room Type
| Room Type | Count | Share |
|---|---|---|
| Room_Type 1 | 28,130 | 77.6% |
| Room_Type 4 | 6,057 | 16.7% |
| Room_Type 6 | 966 | 2.7% |
| Room_Type 2 | 692 | 1.9% |
| Room_Type 5 | 265 | 0.7% |
| (Others) | ~165 | ~0.5% |

**Room_Type 1 accounts for 77.6% of all bookings**, creating a highly imbalanced categorical variable. Room types 5, 7, and others are very sparse and should be consolidated into an "Other" category.

### Meal Plan
| Meal Plan | Count | Share |
|---|---|---|
| Meal Plan 1 | 27,835 | 76.7% |
| Not Selected | 5,130 | 14.1% |
| Meal Plan 2 | 3,305 | 9.1% |
| Meal Plan 3 | 5 | **0.01%** |

**Meal Plan 3 with only 5 records is functionally unusable** as a standalone category and must be merged prior to modeling.

---

## 6. ML Readiness Assessment

### Target Variable
`booking_status` is a **binary classification target** (Canceled / Not_Canceled) with a 32.8% / 67.2% split. This is a moderately imbalanced classification problem.

### Feature Engineering Opportunities

| Opportunity | Details |
|---|---|
| **Total nights** | `no_of_weekend_nights + no_of_week_nights` → single stay length feature |
| **Total guests** | `no_of_adults + no_of_children` → party size feature |
| **Has children** | Binary flag from `no_of_children > 0` |
| **Is weekend-only stay** | `no_of_week_nights == 0` flag |
| **Arrival date encoding** | Combine year/month/date into a proper datetime; extract day-of-week, season |
| **Price per person** | `avg_price_per_room / (no_of_adults + no_of_children)` |
| **Booking history ratio** | `no_of_previous_cancellations / (total previous bookings)` |

### Transformations Required

| Column | Issue | Recommended Treatment |
|---|---|---|
| `no_of_previous_cancellations` | Skewness = 25.2, kurtosis = 732.7 | Log1p transform or ordinal binning (0, 1, 2+) |
| `no_of_previous_bookings_not_canceled` | Skewness = 19.3, kurtosis = 457.4 | Log1p transform or binning |
| `no_of_children` | Skewness = 4.7, kurtosis = 36.98 | Log1p or binary flag |
| `lead_time` | Skewness = 1.29 | Log transform for linear models |
| `avg_price_per_room` | Zero values present | Investigate zeros; consider log transform |
| `arrival_year` | Near-zero variance, 2 values only | Convert to binary (0=2017, 1=2018) |

### Encoding Requirements

| Column | Unique Values | Recommended Encoding |
|---|---|---|
| `type_of_meal_plan` | 4 (but merge Meal Plan 3) | One-hot encoding (3 dummies) |
| `room_type_reserved` | 7 (consolidate sparse types) | One-hot encoding after consolidation |
| `market_segment_type` | 5 (merge Aviation + Complementary) | One-hot encoding |
| `booking_status` | 2 | Binary label encoding (target) |
| `Booking_ID` | 36,275 (unique) | **DROP — identifier only, no predictive value** |

### Algorithm Suitability

| Model Type | Suitability | Notes |
|---|---|---|
| **Gradient Boosting (XGBoost/LightGBM)** | ⭐⭐⭐⭐⭐ Excellent | Handles skewed distributions, outliers, and mixed feature types natively |
| **Random Forest** | ⭐⭐⭐⭐ Good | Robust to outliers; good baseline |
| **Logistic Regression** | ⭐⭐⭐ Moderate | Requires extensive preprocessing (transforms, scaling, encoding) |
| **Neural Network** | ⭐⭐⭐ Moderate | Sufficient data volume; needs careful normalization |
| **K-Nearest Neighbors** | ⭐⭐ Poor | High dimensionality after encoding; slow at scale |

### Class Imbalance Strategy
At 32.8% cancellations, consider:
- **Class weights** in the model (`class_weight='balanced'` in sklearn)
- **Evaluation metric**: Use **F1-score, ROC-AUC, or Precision-Recall AUC** rather than raw accuracy (a model predicting "never cancel" would achieve 67% accuracy trivially)

---

## 7. Recommended Next Steps

### Immediate Actions (Before Any Modeling)

1. **Investigate and handle zero-price rooms**: Run `df[df['avg_price_per_room'] == 0]` and cross-